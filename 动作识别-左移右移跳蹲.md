# 动作识别：左移 / 右移 / 跳 / 蹲（`action/`）

摄像头前的四个动作 → **事件流**。落在《向星而行-UE实现设计.md》§5
「动作识别与门控设计」上，是那一节里**跑在识别进程（Python）**的那部分。

> §5 开头那条总约定照抄在这里，因为整个模块的存在理由就是它
> （`向星而行-任务拆分.md` §1.1）：
>
> **识别侧保证「一次真实动作 = 恰好一个事件」。**
> 游戏侧不做消歧、不做去抖、不做节流、不做方向反转。
>
> 所以迟滞、边沿触发、不应期、优先级仲裁、回位门控**全部在这里**。

## 一分钟跑起来

```bash
cd ~/南客松-v2/GestureMate

.venv/bin/python -m action --demo                # 合成关键点，四个动作各做一遍（不用摄像头/模型）
.venv/bin/python -m action --video clip.mp4      # 真链路：LandmarkEngine → 特征 → 状态机 → 事件
.venv/bin/python -m action --video clip.mp4 --record lm.jsonl    # 顺便把关键点录下来
.venv/bin/python -m action --replay lm.jsonl     # 离线回放（§5.9 要的回归方式）

.venv/bin/python tests/test_action_detect.py          # 26 项合成用例（快、确定性）
.venv/bin/python tests/test_action_detect.py --real    # 再加 1 项真实素材核查
```

`--demo` 的输出（实测）：

```
① 标定：前 2000ms 请站直别动…
   标定完成：62 帧　躯干 0.2000　抖动 0.0000　roll +0.0°
  [    2013 ms] READY  标定完成，可以开始
② 开始识别…（每个动作报一行）
  [    2574 ms] PLAYER 人在画面里
  [    3465 ms] SQUAT  蹲下
  [    4521 ms] SQUAT  站起
  [    5973 ms] LANE   右移
  [    7359 ms] LANE   左移
  [    8712 ms] JUMP   跳
```

**没有「直接开摄像头」这一档**：服务端进程在 macOS 上拿不到摄像头权限（TCC），
`cv2.VideoCapture(0)` 会报 `not authorized to capture video`。命令行只读**文件**；
真机取流走 WebUI，或先录成视频再喂进来。（这条结论见 `交接文档.md` §6.1。）

## 交付的事件

```ts
{ kind: "READY" }                              // 标定完成，可以开始
{ kind: "SQUAT";  on: boolean }                // 锁存态边沿：true=蹲下进入, false=站起
{ kind: "LANE";   dir: "left" | "right" }      // 左移 / 右移（已过镜像与回位门控）
{ kind: "JUMP" }                               // 一次起跳
{ kind: "PLAYER"; present: boolean }           // 人在不在画面里
// 每条都带 t（单调毫秒）与 conf
```

`conf` **只供调试显示**，游戏逻辑不许依赖它（§1.1 原话）。它是"越阈多少"的
归一化：刚压线 0.5，到 1.5 倍阈值 1.0。

本次**只做这四个动作**。`JACK`（开合跳）与 `LEG`（抬腿）契约里留了位但没做 ——
每多做一个就多一份真人重调的成本，而 §5.4(b) 自己写着"第二关一次都用不到抬腿"。

## 四个动作怎么分

同一批下肢关键点，靠**仲裁**分开，不是四个独立检测器：

| 动作 | 判据 | 为什么要这一条 |
|---|---|---|
| **跳** | `rise` 够大，**且横向没怎么动** | 横向够大的那种是换道，不是原地跳 |
| **左移/右移** | `\|laneDx\|` 够大，方向由符号定 | — |
| **蹲** | `sink` 够大 **且两膝都弯** | 只看 `sink` 会把"前倾/后撤"也判成蹲 |

其中（§5.2 的定义，本模块两个名字都给，免得取错符号）：

```
rise = (当前髋高 − 中性髋高) / 躯干      >0 高于中性
sink = −rise                            §5.2 的同一根轴，另一侧
laneDx = 髋中点横向位移 / 躯干           >0 = 玩家向自己的右边
kneeL/R = 髋-膝-踝夹角（度）             180°=伸直
handsUp = 双腕都高于各自同侧肩           §5.2 的 wristAbove
```

> ⚠️ **`sink` 与 `rise` 本来就是同一个量**（互为相反数）。§5.2 把它们列成两行，
> 用的时候最容易在这里取错符号，所以两个都显式给出来 —— 这条在
> `tests/test_action_detect.py` 里有断言钉着（`sink == −rise`）。
>
> **没做**的（§5.2 里有、本次用不到）：`ankleLiftL/R`（抬腿）、`spread/dSpread`（开合跳）。

## 归一化：让阈值与机位、身高解耦

```
原点 = 标定时的中性髋中点
尺度 = 当前帧的躯干长度，但夹在 [0.35, 3.0] × 中性躯干
方向 = 位移投影到标定时刻定下的正交基上
```

**尺度为什么用当前帧、又要夹住**（这条是量出来的，不是拍的）：

* 同一段 120 帧真实素材里，躯干长度在 **0.168 ~ 0.478** 之间变，差 **2.8 倍**
  —— 人往前走两步，画面里的躯干就大一倍。若改用固定的中性躯干当尺度，
  "往镜头走近"会直接变成一次假横移 / 假下蹲；
* 而完全不夹（纯用当前帧）时，人侧身或关键点崩掉（躯干 → 0）会把特征放大到天上。

**方向为什么要投影**：标定时记下「肩中点→髋中点」这个轴当重力反方向，构造
`up = normalize(肩−髋)`、`side = (−up.y, up.x)`，之后所有方向性特征都是位移在
这组基上的投影。这等价于 §5.3.3 说的"扣掉 `rollResidual`"（相机横滚），
但更直接，而且**基是标定时刻定死的，不会因为玩家自己前倾而跟着转**。
测试里把整帧转 15° 后 `laneDx` 仍然是 0.500、`rise` 仍是 0.000。

## 镜像：唯一的那个开关，必须真人验证一次

`TaskController.startListen()` 在推理前把画面水平翻转（自拍视角）：

```python
frame = frame[:, ::-1, :]        # TaskController.py 第 194 行
```

在**翻转过的**画面里，「玩家的右手边 → 画面 +x（x 变大）」。本模块把这件
最容易写反的事收进**唯一一个开关** `mirrored_input`（默认 `True`，与上面的
管线一致），位置固定在「特征提取之后、状态机之前」——正是 §5.7 要求的位置。

* `mirrored_input=True` → 玩家右移 `laneDx > 0`
* `mirrored_input=False` → 玩家右移 `laneDx < 0`

命令行上是 `--no-mirror`。测试里断言了开关一翻左右必须互换。

**好消息**：这里**不会**踩「镜像后 MediaPipe 的解剖学左右标签也跟着反」那个坑 ——
横向位移只用**髋中点**，而髋中点是左右两点的均值，与左右标注无关。

> ⚠️ **但左右对不对，合成数据证明不了（§5.7 原话："合成数据不算数"）。**
> 真人往左移一下，屏幕该报「左移」；报反了就加 `--no-mirror`。
> 这件事**我做不到**，见下面「没验证什么」。

## 门控清单

| 门 | 作用 | 为什么必须有 |
|---|---|---|
| **迟滞** | `sinkOn 0.28 / sinkOff 0.15`、`riseOn 0.12 / riseOff 0.04` | 防临界抖动反复报 |
| **边沿 + 锁存** | 蹲下是锁存态，进一次一个事件；保持期间不重复报，识别到站起才退出 | R6 |
| **不应期 + 回位门控** | 一次瞬时动作后（**含标定结束后的第一次**）**必须回到中性**才能触发下一个；单次换道还有"离开中性到越阈不得超过 0.6s" | §5.4c / §5.5：把"绝对位置"变成**离散边沿事件**，噪声鲁棒性高一个数量级，而且自我重同步 |
| **仲裁** | ① 蹲下期间屏蔽一切瞬时动作 ② 跳要求横向没动 ③（可选）双手过肩锁死换道 | §5.5 |
| **低帧率自适应 `minSamples`** | 条件要连续成立 ≥2 个样本；时间判据按 `max(dwell, (minSamples−1)·dt)` 自动拉长 | 低帧率下"持续 250ms"会退化成"单帧就算过"，抖动直接变成动作 |
| **中值(5) → 1€** | 先杀单帧尖峰，再按速度自适应滤波 | §5.1；1€ 在"压抖动"和"跟快动作"之间平衡最好 |
| **标定可信度** | 标定期髋部抖动 > `calibMaxSpread`(0.06) 就**拒绝标定** | "宁可不标定，也不用假基线" |
| **跟踪断帧即不连续** | 关键点缺失的帧：清滤波状态 + 清全部计时器 + 重新要求回中性 | 实测补上的，见下节 |

> 由此带来一个**使用上的事实**：标定结束后大约 **0.6 秒**内不会触发任何瞬时动作
> （"在场"要连续 good 满 0.5s，回位门控还要再 0.12s）。这是设计使然，不是卡顿。
> **蹲下（姿态锁存）不受这条门限制**，因为它不是瞬时动作。

## ⚠️ 实测：真实素材抓出来的三个缺陷

这三个**不是设想，是真跑出来的**。用 `tests/fixtures/hands_gestures.mp4`
（真人 + 硬切镜头 + 手部特写；全身关键点抖得厉害）跑第一版，报出来假「跳」。

把特征逐帧打出来定位，根因是三条：

1. **接受了一个脏标定。** 那段素材里人根本没站住，标定出来
   `hip_spread = 0.370` 躯干、`stable=False` —— 而第一版算了 `stable` 却**没用它**。
   一个抖 0.37 躯干的"中性"是没有意义的，所有位移特征跟着一起失真。
   → 加 `calibMaxSpread`：超限就拒绝标定并说明原因（`--allow-unstable-calib`
   可以强行放行，仅供调试比对）。
2. **跟踪断了几帧之后重新捕获，被读成了一次极快的位移。**
   实测那次是断 2 帧，回来后 `rise` 一帧跳到 **+0.72**，直接越过跳的门。
   滤波器和计时器在断帧期间仍留着旧样本。
   → 断帧就按"不连续"处理：清滤波状态 + 清全部计时器 + 重新要求回中性。
3. **标定刚结束时是"已武装"状态。** 用不可信基线跑 CLI 时，标定后 800ms
   又误报了一次「跳」。回位门控（§5.5 第 4 条）本来就要求"先回到中性才能触发
   下一个瞬时动作"，但第一版只在**一次动作之后**用它，没用在**标定刚结束**时
   —— 于是标定一结束，只要当前位置离中性够远，下一帧就直接兑现成一个动作。
   → 标定结束也走一遍回位门控：**第一次瞬时动作同样要求先回到中性**
   （真机标定时人本来就站着，这道门 120ms 内就放行，不影响使用）。

修完再量：同一段素材上**默认配置拒绝标定、零动作**；就算强行用脏基线，
也**零动作**（CLI 路径实测确认）。三条行为分别由 PASS 25 / 26 / 27 钉着。

> **第 3 条还带出一个更值得记的教训。** 它一开始**躲过了测试**：那条"真实素材"
> 用例当时用的是 raw `holistic` + `complexity=0` + 固定 33ms，而 CLI 走的是
> `LandmarkEngine`（composed）+ `complexity=1` + 真实 15fps。两者关键点不同，
> 于是**测试通过、CLI 照样误报** —— 正是本项目 README 反复强调的那类 bug
> （"测试替身跟真实接口不一致"，BoxingMate 栽过同一个坑）。
> 修法不是调阈值，而是**把用例改成和 CLI 用同一个引擎、同一个帧率**，
> 它才真的在测真实路径。
>
> 这一段写这么细，是因为它演示了本项目的规矩：
> **合成数据全绿不代表没问题，唯一判据是真材实料地量。**
> 第一版那 26 项合成用例当时**全过了**。

## 与 §5 的有意偏离（逐条给理由）

| # | §5 原方案 | 本实现 | 为什么改 |
|---|---|---|---|
| 1 | 蹲下要求 `kneeL,kneeR < 130°`（§5.4a/§5.6） | `requireKnees` **默认 False**（只靠 `sink`）；拿到世界坐标后可开 | **正面机位下 2D 膝角恒 ≈180°**：髋-膝-踝在画面里几乎共线，这道门永远过不去，开着会把所有正面下蹲全挡掉。§5.1 自己给了出路 —— 要算可靠的下肢角度得开 `outputWorldLandmarks`。两条路都有测试（PASS 20） |
| 2 | 腾空 `rise > 0.06`（§5.6） | 跳的触发门 `riseOn = 0.12` | §5.6 那个 0.06 是给**辅助证据**用的（证明"确有腾空"，配合左右跳/开合跳）。单独当「跳」的门太低：站起来重心一晃、原地小颠一下都能到 0.06 |
| 3 | `wristAbove` 为真时锁死换道（§5.5 第 1/2 条） | `laneBlockWhenHandsUp` **默认 False**（可开） | 那条规则的目的是"别把开合跳的张腿读成横移"，而开合跳是**左右腿对称**张开的 —— 髋中点（本实现用的量）几乎不动。所以这条锁死没有收益，副作用却是漏掉"举着手往旁边跳" |
| 4 | 尺度 = 当前帧躯干（§5.1） | 同左，但**夹在 [0.35, 3.0]×中性** | 实测躯干变化 2.8 倍；不夹会在关键点崩掉时把特征放大到天上 |
| 5 | 未规定「跳」与「换道」的关系 | 跳要求 `\|laneDx\| < jumpMaxLane(0.20) < laneOn(0.35)` | 用户把「跳」单列成一个动作（§5 里腾空只是左右跳的辅助条件），必须让两者由构造互斥，否则一记横向跳会同时报两个动作 |

阈值全表见 `action/detector.py` 的 `ActionConfig`，每个字段都带注释。
**§5.6 那张表自己写着"必须真人重调"，这里同样。**

## 验证到什么程度

**已验证（真跑出来的）**：

* **26 项合成用例**全过，每项盯一条具体的门：静止 3 秒零动作、蹲下锁存、
  蹲下期间屏蔽瞬时动作、左右各命中一次、镜像互换、没回中性不重复触发、
  慢速侧移不算换道、横向跳算换道、单帧瞬移被挡、正面蹲的 2D/3D 两条路、
  断帧不产生动作、脏标定被拒绝、阈值不变式……
* **1 项真实素材核查**：120 帧真人素材（硬切 + 特写）→ 脏标定被拒绝且零动作；
  强行用脏基线也零动作
* **几何自洽性**：合成姿势的膝角能被精确还原（180/150/120/90°），
  `laneDx`/`rise` 的刻度精确；相机横滚 15° 后语义不被带歪；
  走近 1.5 倍后归一化特征不变
* **端到端链路**：`--demo` 四个动作按顺序各报一次；
  `--video` 真跑 LandmarkEngine；`--record`/`--replay` 往返一致

**没验证（需要真人 + 真摄像头，我做不到）**：

* ⚠️ **左右方向有没有反**（§5.7 明确要求真人验证）。开关只有一处，
  真人往左移一下就知道了。
* ⚠️ **阈值合不合手**。`sinkOn 0.28` / `laneOn 0.35` / `riseOn 0.12` 都是按人体
  尺寸估的首版值，没有在真人身上标定过。
* ⚠️ **延迟手感**：1€ 滤波（`fc=1.0, beta=0.007`）会带一点拖尾，
  真人做动作时够不够跟手没试过。嫌钝就调大 `oneEuroFc` 或 `oneEuroBeta`。
* ⚠️ **真人素材覆盖面**：手上只有一段手部特写素材，**没有任何全身动作素材**，
  所以"四个动作在真人身上各自命中几次"这个数字**没有**。
* 蹲下用的是髋高（因为拿不到世界坐标时膝角不可用）。玩家往后站远一点会
  "看起来变小"——尺度夹取能挡住大部分，但真人前后移动时会偏保守。

## 文件结构

```
action/
  __init__.py      对外只暴露 ActionDetector / ActionConfig / ActionEvent
  features.py   ★  关键点 → 特征：归一化、正交基投影、膝角、中值+1€ 滤波、标定
  detector.py   ★  四个动作的状态机 + 全部门控 → ActionEvent
  synth.py         合成关键点（**测试与 --demo 共用一份**，避免替身与实现分叉）
  __main__.py      python -m action：--demo / --video / --replay
tests/
  test_action_detect.py   27 项（26 合成 + 1 真实素材，后者要 --real）
```

`features.py` / `detector.py` **只依赖标准库**（不 import mediapipe / cv2 / numpy），
所以合成用例不需要模型权重、秒级跑完 —— 这也是 BoxingMate「纯函数模块 + node 测试」
那套结构的直接沿用。

顺带一处**纯附加**的改动：`LandmarkEngine.process()` 现在把
`pose_world_landmarks`（米制 3D）也传出来了（它本来就算，只是原实现没往外传）。
这是 §5.1 明确要求的那一项，也是 `requireKnees` 能真正可用的前提。
老代码不读这个字段就不受任何影响。

## 版本库

动手之前先给项目建了版本库（原先没有，README 顶部还专门警告过"改坏了没有回滚"）：

```bash
git log --oneline
#  <本次>  动作识别：左移/右移/跳/蹲
#  bebf10c 基线快照：项目首次纳入版本库
```

当前分支 `feat/action-lane-jump-squat`，`main` 上是那份基线快照，可整体回滚。
`.venv/`（931MB，`run.sh` 会自动重建）与 `.DS_Store` 已加入 `.gitignore`。
