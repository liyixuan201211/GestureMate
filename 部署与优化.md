# GestureMate 部署与 mps 优化记录

> 机器：MacBook Pro · Apple M2 Max / 96 GB / macOS
> 环境：Python 3.12.13 + mediapipe 0.10.14 + opencv-python 4.10.0.84 + numpy 1.26.4
> 日期：2026-09-21

本文只讲两件事：**怎么在这台机器上跑起来**，以及**对 `mps`
（= `mediapipe.python.solutions`，GestureMate 的姿态识别内核）做了什么优化、实测结果如何**。

---

## 一、部署运行

### 1. 一键启动

```bash
cd GestureMate
./run.sh                                  # 摄像头 + ./data/config.json
./run.sh --data="./example/data_example"  # 计算器 demo
./run.sh --video=xx.mp4 --cvshow          # 视频文件当输入，开窗口看骨架
```

`run.sh` 会在缺 `.venv` 时自动用 `uv` 建一个 Python 3.12 环境并装依赖。

### 2. 手工步骤（等价）

```bash
uv venv --python 3.12 .venv
uv pip install --python .venv/bin/python -i https://pypi.tuna.tsinghua.edu.cn/simple \
  -r requirements.txt "PyAutoGUI==0.9.54" "requests==2.32.4"
.venv/bin/python Main.py --data="./example/socket_example"
```

> - **必须是 Python ≤ 3.12**：`mediapipe==0.10.14` 的 wheel 只到 cp312；
>   系统自带的 3.13/3.14 装不上。0.10.30+ 才有 `py3-none-macosx_11_0_arm64` 通用轮子。
> - `requirement.txt`（仓库里文件名少个 s）只列了 3 个包，但官方 demo 还用到了
>   `PyAutoGUI`（16 个 `keypress` 任务）和 `requests`，所以一并装上，否则会一直警告。
> - 首次运行 `Main.py` 会自动下载 MediaPipe 的 `.tflite` 模型权重，需要联网。

### 3. 已验证

| 检查项 | 结果 |
|---|---|
| `mediapipe.python.solutions` 导入 | ✅ `holistic / hands / pose / face_mesh / drawing_utils` 齐全 |
| 端到端（`Main.py --video=...`） | ✅ 120 帧全部处理完，正常退出 |
| 实测吞吐 | **推理 28.3 ms/帧，单帧全流程 32.4 ms（≈30.9 FPS 上限）** |
| 任务链路 | ✅ 日志出现 `processing left / processing right`，detect 任务真的触发了 |
| 回归测试 | ✅ `tests/test_fixes.py` 6 项全过 |

---

## 二、先说一个前提：原版**根本跑不起来**

`tasks/Task.py` 的 `Task.listen()` 原本写的是：

```python
logging.info(f"listening {self.id} type {self.taskType}, ", end="")
```

标准库 `logging` **不接受 `end` 参数**，所以只要有**任何一个任务处于激活状态**，
主循环跑到第 `FPS_COUNT_FRAME * 3 = 30` 帧就会：

```
TypeError: Logger._log() got an unexpected keyword argument 'end'
```

两个官方 demo 的所有任务都是 `start: true`，因此**两个 demo 都会在第 31 帧崩掉**，
计算器和 socket 示例都无法使用。这是本次改动里最重要的一条 —— 不是"优化"，是"能不能跑"。

顺带还有第二个 bug，`tasks/MatchTask.py`：

```python
if delta < self.sensetive[i]:
    self.count[i] += 1
    if self.count[i] >= self.frames[i]:
        self.process(x); return
logging.info(msg)
self.count[i] = 0      # ← 在 if 外面：每帧都清零
```

`frames` 的语义是"连续命中多少帧才算数"，但计数每帧都被清零，
`frames > 1` 时**永远不可能触发**。而计算器 demo 的 `frames` 全是 `4`（还有一个 16）
—— 也就是说即使不崩，计算器的手势也永远不会生效。已把清零移进 `else`。

两处都有回归测试：`tests/test_fixes.py`。

---

## 三、对 mps 做了什么优化（全部有实测）

基准方法：用 **MediaPipe 官方测试数据里的 40 张真实手势照片**做成确定性帧源，
驱动**真正的 `TaskController.startListen()`**，只看吞吐与日志量。
脚本：`tests/bench_mps.py`（管线级）、`tests/bench_engine.py`（引擎级）。

> ⚠️ 测量条件说明：这些数字是在一台**同时还在跑别的任务**的机器上测的
> （当时 `南客松-v2/x5-link` 正在用 ffmpeg 录 2880×1440 屏幕，负载一度到 112，
> 单帧推理被瞬时拉到 62.9 ms）。所以**请重点看同一轮里各方案的相对差异**，
> 绝对值会随机器负载浮动（复测时负载 82 下是 30.1 ms，与低负载时的 28.3 ms 基本一致）。
> 想要干净的绝对值，先确认没有别的重负载进程再跑。

### 3.1 【有效】per-frame 日志与无条件绘制

原代码在每一帧、每个激活任务上都会写日志，而且**不管开不开窗口都会画骨架**：

| 位置 | 原行为 | 现行为 |
|---|---|---|
| `Task.listen` | 每任务每帧 1 条 INFO | `debug` |
| `DetectTask/MatchTask/TimeoutTask/SocketSendTask._listen` | 每帧 1~2 条 INFO | `debug` |
| `TaskController` 主循环 | `logging.info("camera started.")` 每帧 1 条 | 移到循环外 |
| `TaskController.listen` | 每帧 `print("\033[H\033[J")` 清屏 | 只在挂了终端时 |
| 主循环 | `drawLandmarks()` 无条件执行 | 只在 `--cvshow` 时 |

消融实测（1280×720，150 帧，complexity=1，跑两轮）：

| 方案 | 第一轮 FPS | 第二轮 FPS | 日志条数 |
|---|---|---|---|
| A 逐帧日志开 + 绘制开 | 27.6 | 26.4 | 1238 / 1244 |
| B 只关逐帧日志 | 26.9 | 28.3 | 18 |
| C 再关无条件绘制 | **29.8** | **29.8** | 18 |

- 日志量降了约 **70 倍**（1238→18），wall-clock 收益在噪声内（±5%）——
  因为 30 ms 的推理把 1 ms 级的日志开销盖住了。仍然值得改：它是崩溃源头，
  也让 `error.log` 从每帧数条降到只在事件时写。
- **关掉无条件绘制稳定 +5%~11%**，这是外围里唯一确定的正收益。

### 3.2 【重要发现】真正的旋钮是 complexity，不是"少跑模型"

`Main.py` 原来的默认是 `--complexity=2`（heavy）。实测三档（holistic，1280×720，120 帧）：

| complexity | 含义 | 单帧推理 | 中位 | 左右手任一检出 | 相对 |
|---|---|---|---|---|---|
| 0 | lite | **23.0 ms** | 22.3 ms | 89 / 120 | 1.00x |
| 1 | medium | **32.6 ms** | 28.2 ms | 93 / 120 | 0.71x |
| 2 | heavy（原默认） | **76.0 ms** | 74.3 ms | 95 / 120 | 0.30x |

**默认的 2 比 0 慢 3.3 倍、比 1 慢 2.3 倍，而检出只多 2 张。**
因此把默认改成 **`--complexity=1`**（要极限精度仍可显式 `--complexity=2`）。

### 3.3 【被实测否掉】"只跑手模型"是负优化

很自然的想法：官方 demo 只用到 `leftHand`/`rightHand`，那就别跑人脸(468点)和人体，
只留手部模型。`LandmarkEngine` 实现了这条路线（`--engine=minimal`），
**但实测是负优化**：

| 方案 | 单帧推理 | 左右手任一检出 |
|---|---|---|
| holistic（原版） | **28.6 ms** | **93 / 120** |
| 只跑 hands | 32.1 ms | 84 / 120 |

原因：Holistic 会用**人体姿态**推出手部 ROI 再做关键点；而独立的 `hands.Hands`
每帧都要在整张图上跑一遍手掌检测。省了人脸/人体，却多了全图手掌检测 —— 净亏。

所以 `--engine` 默认是 **`holistic`**，`minimal` 只作为可选项保留，
方便换机型/换 mediapipe 版本后用 `tests/bench_engine.py` 复测再决定。

### 3.4 【无收益】推理前降采样

`--proc-size=640`（把长边缩到 640 再送进 MediaPipe）：29.5 vs 29.8 FPS，**在噪声内**。
MediaPipe 内部本来就会缩到 256/224 级别，外层再缩一次省下的拷贝很有限。
而且它会让检出从 84 掉到 81。**默认已改为 0（不缩放）**，仅作为可选项保留。

---

## 四、改动的文件

| 文件 | 改动 |
|---|---|
| `tasks/Task.py` | 修 `logging.info(..., end=)` 崩溃；逐帧日志降为 debug |
| `tasks/MatchTask.py` | 修 `count` 每帧被清零（`frames>1` 永不触发）；逐帧日志降为 debug |
| `tasks/DetectTask.py` / `TimeoutTask.py` / `SocketSendTask.py` | 逐帧日志降为 debug |
| `TaskController.py` | 日志/清屏/绘制只在需要时；`--video`、`--proc-size` 支持；推理均值统计 |
| `Utils.py` | `extractLandmarks(x, need=...)` 只转换配置用到的部位（**左右手镜像补偿保持不变**） |
| `LandmarkEngine.py` | 新增：Holistic / 按需组合两条路线的统一封装（默认 holistic） |
| `Main.py` | 新增 `--video` / `--proc-size` / `--log-level` / `--engine`；默认 complexity 2→1 |
| `run.sh` | 一键建环境并启动 |
| `tests/bench_mps.py` | 管线级基准 + 消融矩阵 |
| `tests/bench_engine.py` | 引擎级对比 + complexity 扫描 |
| `tests/test_fixes.py` | 两个 bug 的回归测试（含镜像补偿不被破坏） |
| `tests/fixtures/hands/` | 40 张 MediaPipe 官方手势测试图（基准帧源） |

---

## 五、复现命令

```bash
cd GestureMate

# 1) 两个 bug 的回归测试
.venv/bin/python tests/test_fixes.py

# 2) 管线级消融（原版会崩在第 31 帧，优化后跑满 150 帧）
.venv/bin/python tests/bench_mps.py --srcdir . --baseline tests/baseline --frames 150 --matrix

# 3) complexity 扫描（决策依据）
.venv/bin/python tests/bench_engine.py --sweep --frames 120

# 4) 真实入口端到端（不需要摄像头）
.venv/bin/python Main.py --data tests/_e2e_data \
    --video tests/fixtures/hands_gestures.mp4 --fps 0
```

---

## 六、还没做 / 需要你确认的

1. **摄像头权限**：默认模式用 `cv.VideoCapture(0)`。从终端/Agent 拉起时 macOS 可能
   弹权限框（或直接给空帧）。第一次请在有屏幕的终端里手动跑一次 `./run.sh` 并授权。
2. **`pynput`/AppleScript 权限**：计算器 demo 用 `keypress` 任务（pyautogui），
   macOS 需要在「辅助功能」里给终端授权才能真的发按键。
3. **360/其他机型复测**：`--engine=minimal` 与 `--proc-size` 的结论只对
   M2 Max + mediapipe 0.10.14 有效，换环境请重跑 `tests/bench_engine.py`。
4. 上游仓库是 GPL-3.0，本目录保留原始 LICENSE。
