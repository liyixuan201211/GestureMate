# 交接文档 —— GestureMate WebUI + 全景跟随

> 写给接手的人（或下一个 agent）。目标：**不读代码也能把它跑起来**，
> 并且知道哪些是真验证过的、哪些只是"能跑"、坑在哪里。

最后更新：项目从 `~/南客松/GestureMate` 移到 `~/南客松-v2/GestureMate` 之后。

---

## 0. 一句话现状

在上游 [interfacew/GestureMate](https://github.com/interfacew/GestureMate)（Python + MediaPipe
手势识别 + 桌面自动化）之上加了两层：

1. **WebUI（浏览器取流 + WebRTC）** —— 已能用，默认推理在浏览器里跑
2. **全景跟随（`pano/`）** —— 在 X5 的等距柱状全景里找到人、跟住他、对他做动作检测。
   已在 **X5 实拍帧**上验证并修好（原来对真人一帧都锁不住，见 §4 / §7.2）。

上游的识别算法、任务系统、配置格式**没有被改语义**（只修过 bug）。

```bash
cd ~/南客松-v2/GestureMate
./run_webui.sh                       # 起服务，然后浏览器开 http://127.0.0.1:8770
```

---

## 1. 五分钟跑起来

### 1.1 WebUI（最常用的入口）

```bash
cd ~/南客松-v2/GestureMate
./run_webui.sh                       # 默认 --infer browser
# 浏览器打开 http://127.0.0.1:8770 → 点「开始」→ 允许摄像头
# 首次会下载/加载约 45MB 的 MediaPipe 模型与 WASM，等几秒
```

`run_webui.sh` 会自己检查 venv、装缺的依赖（aiortc/aiohttp），然后起服务。

**注意**：`http://127.0.0.1` 是安全上下文，摄像头可用；
换成 `http://<局域网IP>:8770` 时浏览器会拒绝 `getUserMedia`（不是 bug）。

### 1.2 命令行版（上游原有）

```bash
./run.sh example/data_example        # 需要摄像头权限，见 §6.1
```

### 1.3 全景跟随

```bash
# 用 3dgs-lab 里现成的等距柱状帧试跑
.venv/bin/python -m pano \
  --frames ~/南客松-v2/3dgs-lab/proj_erp/frames \
  --out /tmp/pano_out --limit 8

# 或吃一个等距柱状视频
.venv/bin/python -m pano --video pano.mp4 --out /tmp/pano_out
```

### 1.4 跑测试（改完代码必跑）

```bash
cd ~/南客松-v2/GestureMate
for t in tests/test_fixes.py tests/test_face_body.py \
         tests/test_landmark_engine.py tests/test_pano_follow.py \
         tests/test_pano_real_frame.py; do
  .venv/bin/python "$t"
done
node tests/test_extrapolation.mjs
```

全部应当是「全部通过」。**这 6 个测试是这套东西唯一的回归网**（项目没有 git，见 §7.1）。
其中前 5 个是自己造数据/合成数据，`test_pano_real_frame.py` 用的是 **X5 实拍帧**
（见 §4）。

---

## 2. 目录地图

```
GestureMate/
├── run_webui.sh              ← WebUI 入口（自建 venv、装依赖、起服务）
├── run.sh / Main.py          ← 上游命令行入口
├── LandmarkEngine.py         ← 上游：mps 引擎封装（holistic / 分模型）
├── Utils.py                  ← 上游：extractLandmarks（含镜像补偿的关键点提取）
├── TaskController.py         ← 上游：任务系统（detect/match/keypress/command/socket…）
├── tasks/                    ← 上游：各种任务的实现
├── face_body.py              ← 新增：眼睛/嘴巴/头朝向/举手 的语义（EAR/MAR/头姿）
├── example/, data/           ← 任务配置与录制的姿态文件
│
├── webui/                    ← 新增：WebUI
│   ├── server.py             ← aiohttp + aiortc；--infer browser|server
│   ├── pipeline.py           ← GesturePipeline（服务端 mps）+ LandmarkTaskEngine（关键点模式）
│   ├── gen_connections.py    ← 生成前端要用的关键点拓扑表
│   └── static/
│       ├── app.js            ← 前端主逻辑（取流/推理循环/叠加层/面板）
│       ├── infer.js          ← 浏览器端 MediaPipe Tasks 封装（ES module）
│       ├── index.html, style.css
│       └── mediapipe/        ← 45MB：Tasks 运行时 + WASM + 4 个模型（见 §7.2）
│
├── pano/                     ← 新增：全景跟随与动作检测
│   ├── projection.py         ← 等距柱状 ↔ 透视 投影（core 取自 3dgs-lab/x360）
│   ├── follow.py             ← PanoFollower：找人 / 跟住 / 动作检测
│   ├── __main__.py           ← 命令行
│   └── demo/                 ← 三张视觉对照图 + real_x5/（X5 实拍帧与诊断图）
│
├── tests/                    ← 回归测试（6 个）
├── WebUI-RTC.md              ← WebUI 的架构与全部实测数据、踩坑记录
├── 全景跟随与动作检测.md        ← pano/ 的说明
├── 部署与优化.md               ← 上游 mps 的性能优化记录
└── 交接文档.md                 ← 本文件
```

---

## 3. 架构：两条推理路径

### 3.1 `--infer browser`（默认）

```
浏览器  --getUserMedia-->  <video> ──► MediaPipe Tasks ──► 画骨架（同一帧！）
                                    └── 关键点 --DataChannel--> 服务端
服务端  --DataChannel-->  语义特征 + 任务事件  -->  浏览器
```

* **视频一步都不出浏览器**（实测 `pc.getSenders() == []`）。
* 叠加层画在**刚算过的那一帧**上 → 对齐是构造上成立的，不靠外推。
* 服务端只干两件便宜事：`face_body` 语义 + `TaskController` 任务系统（实测 **0.05ms/帧**）。
* 浏览器推理实测 **21.3ms/帧（GPU，1280×720）→ 46.9 FPS**，比服务端 mps 还快。

### 3.2 `--infer server`（旧路，保留）

```
浏览器 --RTP 视频上行--> 服务端 mps --> 关键点 --DataChannel--> 浏览器
```

命令行版（`Main.py`）走的就是这条，未改动。
帧源还支持 `--source opencv|ffmpeg|file`（给服务进程授了摄像头权限后可用）。

### 3.3 全景跟随（`pano/`）

```
等距柱状全景 ──► ① 找人/跟住（方位 yaw/pitch）──► ② 抽出该方位的透视画面
                                                      └──► GestureMate 原有检测（脸/身/手 + 语义）
```

**核心判断**：**绝不能把整张全景丢给人形检测器** —— 等距柱状图左右两端拉伸最厉害，
人在那儿会被拉畸形、检不出来。所以全景只用来**抽视角**，检测器只吃矫正后的透视画面。

---

## 4. 已验证到什么程度（证据，不是"应该能跑"）

| 项目 | 证据 |
|---|---|
| 浏览器内推理 | 引擎加载正常、零 JS 报错、`pcSenders=[]`、关键点持续上行 253 帧、服务端 0.05ms/帧 |
| 服务端 mps 模式 | `tests/rtc_client.py --fps 30`：ICE completed / DataChannel open / 状态 26/s / 结果延迟 0.1ms |
| 面部+身体 | 真实浏览器：脸 468 点、身体 33 点、眨眼计数、头朝向、举手 均正常 |
| 全景跟随 | `tests/test_pano_follow.py` 6 项：**贴 +70/−110/0/155° 最大误差 1.4°**、空场景不误锁 |
| **全景跟随（真机帧）** | `tests/test_pano_real_frame.py` 4 项，用 **X5 实拍 2880×1440 帧**：锁定 yaw+101.8°/pitch+48.4°、score 0.85、**脸 468 + 身体 33**、语义齐；跟住一帧 **74ms 定位 + 29ms 检测** |
| 全景动作检测 | 视觉 demo（`pano/demo/`）：找到 −93.9°、检出脸 468 + 骨架 + 举手 left=True |
| 延迟优化 | 单帧 36.5→29.5ms、推送延迟 19→0.1ms、前端更新率 10→30/s（见 `WebUI-RTC.md` 七） |

**没验证到的**（要诚实）：

* 真人站在全景里的**实时常驻循环** —— 真机帧这一关过了（上面那一行），
  但"从 X5 连续取流喂进 `step()` 的常驻进程"还没写（差一个循环，见 §7.2）。
  ⚠️ 补验真机帧时发现**原来的实现对真人一帧都锁不住**，已修；细节见
  `全景跟随与动作检测.md` 第三、六节。
* **画面"翻滚"还没解**：X5 在桌上时它的"上"≠重力的"上"，抽出来的透视画面是歪的
  （实测人体轴相对画面竖直线偏约 20°，天地线也是斜的）。后果是 `face_body` 里
  「身体倾斜 / 头 roll / 举手方向」这类**方向性语义是相对歪画面的**，数值不能直接信。
  修它要先估重力方向。**这是"在房间平面/墙面上贴信息"那一步的前置条件。**
* 浏览器端推理只在 Chromium（ego lite）实测过；**Safari / Firefox 没测**。
* macOS 上服务端直接开摄像头仍然不行（TCC），只验证过 `--source file`。

---

## 5. 关键约定（改代码前务必知道）

### 5.1 坐标系与镜像（最容易搞错）

* 上游推理前**会把画面水平镜像**，再在 `Utils.extractLandmarks` 里**把左右手对调**补偿。
* WebUI 统一把关键点还原到**相机原始（未镜像）坐标系**再发布（`pipeline.py::_unmirror`），
  前端把 video 和 canvas **一起用 CSS 镜像**出"自拍感"。
* **别去"修"这个镜像逻辑**：手部的左右对调是必须的；脸部则按 x 顺序判左右
  （`face_body.py::eye_groups`），不依赖标签。

### 5.2 手性（handedness）—— 已经踩过坑

`webui/static/infer.js` 里 `SWAP_HANDEDNESS = false`。

* MediaPipe 文档说手性假定"输入已镜像"，非镜像输入要自行互换 ——
  **照那条做是错的**（真人举左手会被标成 `rightHand`）。实测结论是不互换。
* ⚠️ 当时我用 fixture 图对照"浏览器(原图) vs 旧管线(镜像+swap)"得到"完全一致"
  就下了结论 —— 那只证明两个实现**一致**，不证明它们**正确**。参考基准本身就是错的。
  真正的判据只有**真人举手**。
* **副作用**：旧管线 `extractLandmarks` 对调过左右手，所以它录制的姿态文件
  （`example/data_example` 里 `GetPoseJson` 生成的那批）**标签是反的**。
  用新语义跑那些配置时需要用另一只手，或者重新录一遍。**这件事还没做。**

### 5.3 全景的角度约定

`pano/projection.py` 沿用 `3dgs-lab/x360` 的约定（已在那边数值验证过）：

```
pano 坐标系：+X 右, +Y 上, +Z 前（yaw=0 的视线方向）
yaw   绕 +Y，0 = 看 +Z，正值 = 向右转
lon = atan2(d.x, d.z)   lat = asin(d.y)
xs = (lon/2pi+0.5)*(w-1)   ys = (0.5-lat/pi)*(h-1)
```

**中心在半个像素上**：`lon=0` 对应 `x=(w-1)/2`，整数 `w/2` 偏 0.176°。

### 5.4 其它几条容易踩的

* **不要对角度直接做 EMA** —— 359° 和 1° 会平均成 180°。要在 (sin,cos) 上平滑
  （`follow.py::_circular_smooth`）。
* **镜像必须用 `cv2.flip`**，别用 `frame[:, ::-1]` 的负步长视图：
  后者 `ascontiguousarray` 要逐元素拷，1280×720 实测 **3.93ms vs 0.39ms**。
* **`threading.Thread` 的 `_stop` 别覆盖** —— 上游曾把停止标志命名为 `_stop`，
  撞上 `Thread._stop()`，一重启就 `TypeError`。现已改名 `_stopEvent`。
* **管线里不要 `close()` 帧源** —— 管线会被重启（切配置、看门狗），
  帧源生命周期归服务端管。
* **状态推送要事件驱动**，别固定 10Hz 轮询：实测平均白等 19ms、且前端每秒只更新
  10 次（视频 30fps）→ 看着就是"跟不上手"。

---

## 6. 环境与依赖

### 6.1 macOS 摄像头权限（重要）

**dsh 所在进程树没有摄像头权限**（TCC）：OpenCV 报
`not authorized to capture video (status 0)`，ffmpeg/AVFoundation 会卡死并被 SIGKILL。

所以取流**必须放在浏览器**（浏览器是正规 GUI App，点一下"允许"就行）。
如果哪天给终端/服务进程授了权限，`--source opencv|ffmpeg` 就能用。

### 6.2 Python 环境

* `.venv` 是 **Python 3.12.13**，因为 `mediapipe 0.10.14` 的 wheel 只到 cp312。
* 关键依赖：mediapipe 0.10.14 / opencv-python 4.11 / numpy 1.26.4 /
  aiortc 1.15 / aiohttp 3.14 / PyAutoGUI（上游需要）。
* **venv 可以整体搬迁**（这次从 `~/南客松/GestureMate` 搬到
  `~/南客松-v2/GestureMate` 后实测可用：`prefix` 自动指向新路径）。
  但 `bin/` 下的 console script（pip 之类）shebang 是绝对路径，可能失效 ——
  直接用 `./.venv/bin/python` 调用没问题。

### 6.3 前端模型资源（45MB）

`webui/static/mediapipe/`：

| 文件 | 大小 | 说明 |
|---|---|---|
| `wasm/vision_wasm_internal.{js,wasm}` | 9.7MB | SIMD 版 |
| `wasm/vision_wasm_nosimd_internal.{js,wasm}` | 9.6MB | 非 SIMD 回退 |
| `vision_bundle.mjs` | 140KB | MediaPipe Tasks JS |
| `models/face_landmarker.task` | 3.8MB | |
| `models/pose_landmarker_lite.task` | 5.8MB | 默认 |
| `models/pose_landmarker_full.task` | 9.4MB | 「姿态模型」选 full 时用 |
| `models/hand_landmarker.task` | 7.8MB | |

**能省 ~9.6MB**：如果确定目标机器都支持 SIMD，可以删掉 nosimd 那两个。
（服务端已注册 `.mjs→text/javascript`、`.wasm→application/wasm`、`.task→octet-stream`
的 MIME，否则浏览器会拒绝加载 ES module。）

---

## 7. 已知问题与下一步

### 7.1 项目没有 git ⚠️

`git status` 报"不是 Git 仓库"。**没有任何版本回滚能力**，改坏了只能靠
`tests/` 那 5 个测试兜。建议第一件事就是 `git init` + 首次提交。

### 7.2 还没做的事（按建议优先级）

1. **给项目建 git 仓库**（见上）。
2. **旧姿态文件的左右手标签**（§5.2）：要么重录 `example/data_example` 那 24 个
   `.json`，要么加一个兼容开关。否则计算器 demo 需要用"反的那只手"触发。
3. **接 X5 实时流（常驻循环）**：`pano/` 只要求"一帧等距柱状图"，x5-link 的
   `web --port 8765` 已经在推 MJPEG。**取帧这条路已经实测通了**
   （`curl -s http://127.0.0.1:8765/snapshot.jpg -o f.jpg` → 直接喂 `step()`，
   §4 那行真机数据就是这么来的）。还差的只是把它写成**一个常驻进程**
   （复用同一个 `PanoFollower`，别每帧新建 —— 那样每帧都要重新找人，2 秒一帧）。
4. **估重力方向 / 解掉画面翻滚**：见 §4 那条。这是"在房间平面/墙面上贴信息"
   的前置条件 —— 要贴到地面/墙面上，本来就得有一个重力对齐的坐标系。
   候选做法：用检出的人的「肩中点→髋中点」轴当重力参考（站立的人这条轴就是重力
   方向），或者从地平线/消失点估。⚠️ 实测髋部常常被 MediaPipe 外推到画面外，
   所以还得配合"只用画面内关节"这条（`pano/follow.py` 里已经这么做了）。
5. **第 ② 步：在真实房间的平面（地面/墙面）上标出这个人的各种信息**
   （产品需求原话："在一个平面当中给这个人的各种信息标记出来"）。
   依赖 4，因为需要一个重力对齐的坐标系 + 单目尺度估计（人的身高 / 脚的位置）。
6. **把跟随的 yaw/pitch 反喂给 `x5link/viewer.html`**，做到"网页视口自动跟着人转"。
   这需要改 x5-link（另一个 agent 的工程），本次没动。
7. **浏览器端推理的兼容性**：只测过 Chromium。Safari 的 WebGL/WASM 行为不同，
   建议至少手动过一遍。
8. **冷启动**：首次推理可能十几秒（WebGL 着色器编译）。目前是"先建引擎再开推理循环 +
   显示加载态"，但仍可以做得更明确（进度条）。
9. `--infer server` 模式下的「服务端视角 / 快照」在浏览器模式下已隐藏，
   如果以后两个模式都要用，UI 的切换逻辑在 `app.js` 里按 `inferMode` 分支。

### 7.3 报告数据时的注意事项

机器负载波动很大（本次会话中曾到过 54，原因是反复起浏览器测试进程）。
**优先报"同一次运行内的相对差异"**，别把绝对帧率当稳定指标。

---

## 8. 与 `南客松-v2` 里其它项目的关系

| 项目 | 关系 |
|---|---|
| `x5-link/` | **只读参考**。本项目的 `pano/` 依赖它的两条情报：X5 的 Webcam 模式吐 2880×1440 等距柱状图；README 第八节指出"按朝向抽透视视角即可，人体朝向可直接驱动视角"。**没有改动它任何文件。** |
| `3dgs-lab/x360/projection.py` | **只读参考**。等距柱状→透视 的投影数学从这里**复制**（不是跨项目 import，那样太脆），复制后补了两个反向映射并加了往返一致性测试。 |
| `3dgs-lab/STATUS.md` | 记录 360 多视角 SfM 还卡着。那是**离线重建**用的，与实时取流互不影响。 |

---

## 9. 深入阅读顺序

1. 本文件（跑起来 + 知道坑在哪）
2. `WebUI-RTC.md` —— WebUI 的架构、全部实测数据、7 条踩坑记录（含那次"卡死"的完整成因）
3. `全景跟随与动作检测.md` —— 全景跟随的算法取舍与实测
4. `部署与优化.md` —— 上游 mps 的性能优化（复杂度/分辨率扫描等）
5. 代码：`webui/app.js` 与 `webui/pipeline.py` 是前端/服务端的主干；
   `pano/follow.py` 是全景跟随的主干。
